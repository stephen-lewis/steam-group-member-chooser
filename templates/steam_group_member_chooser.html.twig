{% extends 'base.html.twig' %}

{% block body %}
    {% if form is defined %}
    {{ form_start(form) }}
    {{ form_end(form) }}
    {% else %}
    The winner is {{ winner }}.
    {% endif %}
{% endblock %}
