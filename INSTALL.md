Clone into appropriate web directory.

Run `composer install` to install Symfony.

Update .env with your [Steam API Key](https://steamcommunity.com/dev/apikey).

Point external URL to `/public/`.

Pick winners.
